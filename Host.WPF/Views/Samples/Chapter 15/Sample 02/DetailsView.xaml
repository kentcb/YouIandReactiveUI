<rxui:ReactiveUserControl x:Class="Book.Views.Samples.Chapter15.Sample02.DetailsView"
        x:TypeArguments="vms:DinosaurViewModel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:rxui="http://reactiveui.net"
        xmlns:vms="clr-namespace:Book.ViewModels.Samples.Chapter15.Sample02;assembly=Book.ViewModels">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label
            Content="Name:"/>
        <Label
            x:Name="nameLabel"
            Grid.Column="1"/>

        <Label
            Content="Diet:"
            Grid.Row="1"/>
        <Label
            x:Name="dietLabel"
            Grid.Row="1"
            Grid.Column="1"/>

        <Label
            Content="Period:"
            Grid.Row="2"/>
        <Label
            x:Name="periodLabel"
            Grid.Row="2"
            Grid.Column="1"/>

        <Label
            Content="Era:"
            Grid.Row="3"/>
        <Label
            x:Name="eraLabel"
            Grid.Row="3"
            Grid.Column="1"/>
    </Grid>
</rxui:ReactiveUserControl>